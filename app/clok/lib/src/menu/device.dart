/// A placeholder class that represents an entity or model.
class DeviceItem {
  const DeviceItem(this.deviceID, this.optionName, this.optionValue);

  final String deviceID;
  final String optionName;
  final String optionValue;
}
